{% extends 'base.html.twig' %}

{% block title %}Contribution{% endblock %}

{% block body %}
    <h1>Contribution</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contribution.id }}</td>
            </tr>
            <tr>
                <th>Somme</th>
                <td>{{ contribution.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contribution_index') }}">Retour à la liste</a>

    <a href="{{ path('contribution_edit', {'id': contribution.id}) }}">edit</a>

    {{ include('contribution/_delete_form.html.twig') }}
{% endblock %}
